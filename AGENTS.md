# Agent Instructions — mwa-recharge-content

See CLAUDE.md for full content authoring instructions. This file ensures compatibility with other AI coding agents (Codex, Gemini, etc.).

## Summary

- Write Markdown articles with YAML frontmatter
- Place in `articles/{primary-tag}/{slug}.md`
- Follow the schema in CLAUDE.md exactly
- Commit: `content: add {slug}` or `content: update {slug}`
- Open a PR for human review before merging to main
